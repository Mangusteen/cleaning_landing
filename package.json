{
  "name": "cleaning_landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.75.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}